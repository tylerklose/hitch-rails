# Work packet: M4.5

## Scope

Execute the exhaustive twelve-row Lattice authorization contract plus every
forced hostile boundary suite. Add focused mutation coverage for audience,
admission, registration, availability, scope, schema, policy, execution,
result/redaction, context, callbacks, and observation. Prove simultaneous
principals and reload-during-traffic isolation before accepting the internal
`0.2.0.pre.3` checkpoint.

## Not in scope

- Weakening/remapping tests to satisfy mutation results.
- Generator/disposable-app work, adoption, paid clients, or new public API.
- Calling capability-gated or unimplemented behavior a pass.

## Target files/API

- executable `test/integration/mcp_tool_authorization_lattice_test.rb` and forced
  boundary/concurrency suites.
- mutation configuration, `bin/mutation-mcp` (new), and a minimal
  `bin/ci-generators` dispatcher whose install/tool targets are implemented by
  M5.1/M5.2 without cross-branch command ownership.
- consolidated contract and evidence manifest checks.

No new public API; this packet proves M2-M4 behavior.

## Dependencies

M4.4 observation and every earlier runtime boundary are accepted.

## Acceptance commands

Run:

```sh
bin/contract
bin/mutation-mcp
bin/ci-sdk min
bin/ci-sdk latest
```

All twelve paths and forced suites pass; every in-scope mutation is killed with
zero unexplained timeout/alive result and no changed generated scenario file.

## Evidence paths

Create `docs/evidence/0.2.0/quality/m4-contract-mutation.json` with scenario IDs,
forced-suite outcomes, subject/mutation counts, survivors/timeouts, versions,
and artifact checksums.

## Rollback

R1: supersede the internal checkpoint only after rerunning the full gate. A
weakened test or mutation map requires the same full gate as runtime changes.

## Estimate

Four engineering days. Risk is high because this is the integrated security
proof before the Rails golden path.

## Risks

- Happy-path coverage can miss precedence mutations.
- Untracked subjects can escape diff-selected mutation runs.
- Concurrency tests can pass without overlapping requests.
- Generated scenarios can be hand-edited after coverage calculation.

## Owner

The Hitch release maintainer owns mutation scope, survivor adjudication, and
internal `0.2.0.pre.3` checkpoint acceptance.
