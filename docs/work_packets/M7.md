# Work packet: M7

## Scope

With Tyler-granted access, adopt Hitch in Stash or another approved tools-only
host whose MCP design did not descend from Skillit, Kaffe Karma, or Perfect
Roofing. Map its existing visibility and record policy into Hitch's two gates,
turn friction into failing Hitch acceptance tests before framework changes, and
prove tool/principal/reload isolation before `0.2.0.rc2`.

## Not in scope

- Entering a private host without approval, inventing a Hitch policy framework,
  weakening host policy, or relabeling a copied host independent.
- Treating deployment success alone as API validation.

## Target files/API

- Approved independent host changes and private adoption report.
- Hitch acceptance tests for independently repeated friction.
- redacted provenance, isolation, semantic parity, and rollback evidence.

Incompatible redesign returns to M3/M4 and repeats both adoptions; it does not
receive a hidden compatibility shim.

## Dependencies

M6/`0.2.0.rc1` are accepted. This packet is blocked until Tyler grants access
and approves Stash or a demonstrably independent substitute. RubyGems
availability is not a dependency; a deferred public train uses the exact
accepted internal RC artifact.

## Acceptance commands

Run the host-owned CI, MCP smoke, and isolation commands documented in the
private report. Then run:

```sh
bin/contract
bin/mutation-mcp
```

Any framework change reruns both M6 and M7 acceptance evidence.

## Evidence paths

Create `docs/evidence/0.2.0/adoption/independent.json` with approval,
independence proof, host commit hash, redacted results, friction disposition,
isolation, cutover, and rollback.

## Rollback

R3: restore the host's prior canonical route/deploy atomically while preserving
the isolated preview for diagnosis.

## Estimate

Five engineering days after access. Risk is high because independent evidence
can force a public redesign.

## Risks

- Shared ancestry can be missed and overstate independence.
- Framework abstractions can displace valid host-owned policy.
- A single adoption request can overfit public API.

## Owner

Tyler Klose owns host/access/deployment/substitution approval. The release
maintainer owns framework disposition and `0.2.0.rc2` checkpoint acceptance.
RC publication is allowed only if M5 recorded `published_pre4` and separate
explicit publication authority is granted.
