#!/usr/bin/env ruby
# frozen_string_literal: true

PACKAGE_SMOKE = File.expand_path("package-smoke", __dir__)

unless ARGV.empty?
  warn "Usage: bin/package-apps"
  exit 64
end

ENV.delete("HITCH_AUTOMATED_CLIENT_SMOKES")
exec PACKAGE_SMOKE
