# Work packet: M8

## Scope

Stabilize one immutable `0.2.0` artifact and one honest public contract. Reconcile
API/migration/security/release docs and evidence index; run full CI/matrix/
conformance/package/client/mutation/adoption gates; retain visible capability
skips and two documented untestable probes; download and verify RubyGems bytes.

## Not in scope

- Hiding skips/failures/baselines, rewriting published tags/gems, silently
  dropping supported lanes, or adding last-minute public API without repeating
  its owning milestones.
- Publishing without maintainer and Tyler-owned adoption approvals.

## Target files/API

- final changelog/API/security/upgrade/removal/release docs and evidence index.
- release automation/checklist and downloaded-gem verifier.
- all public constants/configuration/generators/events/task output already
  frozen in the manifest; everything else remains private.

The released gem/tag/docs/checksums must identify the same bytes and contract.

## Dependencies

M7/`0.2.0.rc2` and both adoption approvals are accepted. No unresolved blocker,
dual route, stale baseline, hidden database/client requirement, or unowned
security control remains.

## Acceptance commands

Run:

```sh
bin/ci
bin/contract
bin/package-apps
bin/client-smokes --automated
bin/release-check 0.2.0
```

Also run the full named conformance and mutation gates and approved product-
client smokes. No unexplained skip, unexpected failure, or stale baseline entry
is permitted.

## Evidence paths

Create `docs/evidence/0.2.0/release/final-check.json` and
`docs/evidence/0.2.0/release/downloaded-gem.json` with approvals, all gate
outcomes, artifact/tag/checksums, explicit skips/probes, and evidence index.

## Rollback

R4: if necessary yank the affected release and forward-patch under a new
version. Never rewrite a tag or published artifact.

## Estimate

Three engineering days plus maintainer publication/approval time. Risk is high
because this converts evidence into an immutable support claim.

## Risks

- A green aggregate can hide an excluded lane or stale allowlist.
- Documentation can outlive the final mechanism.
- A release check against the checkout can miss downloaded-byte drift.

## Owner

The Hitch release maintainer owns final evidence, release, and publication;
Tyler Klose owns required adoption and product-smoke approvals.
